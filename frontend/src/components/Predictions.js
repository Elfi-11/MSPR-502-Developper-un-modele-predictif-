import React from 'react';

const Predictions = () => (
  <main style={{ padding: '2rem' }}>
    <h1>Prédictions</h1>
    <p>Cette page affichera les prédictions générées par le modèle.</p>
    {/* TODO: Intégrer le composant Dashboard existant ou créer une nouvelle vue prédictions */}
  </main>
);

export default Predictions;
